
PROGRAM NO: 12 :Automate CI/CD with GitHub Actions	
 
SOURCE CODE:

name: CI — frontend & backend tests

on:
pull_request:
push:
branches: [ develop, feature/** ]

jobs:
frontend:
runs-on: ubuntu-latest steps:
-	uses: actions/checkout@v4
-	uses: actions/setup-node@v4 with: node-version: 20
-	name: Install deps
working-directory: frontend run: npm ci
-	name: Lint
working-directory: frontend run: npm run lint --if-present
-	name: Tests
working-directory: frontend
run: npm test --if-present	watchAll=false

backend:
runs-on: ubuntu-latest services:
postgres:
image: postgres:15 env:
POSTGRES_USER: postgres POSTGRES_PASSWORD: postgres ports: ['5432:5432']
options: >-
--health-cmd pg_isready
--health-interval 10s
--health-timeout 5s
--health-retries 5 steps:
-	uses: actions/checkout@v4
-	uses: actions/setup-node@v4 with: node-version: 20
-	name: Install deps
working-directory: backend run: npm ci
-	name: Lint
working-directory: backend
 
run: npm run lint --if-present
-	name: Tests
working-directory: backend env:
DATABASE_URL: postgres://postgres:postgres@localhost:5432/postgres run: npm test --if-present

name: Backend CD — build, push, deploy to ECS on:
push:
branches: [ main ]

env:
IMAGE_TAG: ${{ github.sha }}

jobs:
build-push:
runs-on: ubuntu-latest steps:
-	name: Checkout
uses: actions/checkout@v4

-	name: Login to AWS
uses: aws-actions/configure-aws-credentials@v2 with:
aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }} aws-region: ${{ secrets.AWS_REGION }}

-	name: Configure Docker Buildx uses: docker/setup-buildx-action@v2

-	name: Login to ECR id: login-ecr
uses: aws-actions/amazon-ecr-login@v1

-	name: Build and push image to ECR uses: docker/build-push-action@v4 with:
context: backend push: true
tags: ${{ secrets.ECR_REGISTRY }}/${{ secrets.ECR_REPOSITORY }}:${{ env.IMAGE_TAG }}
cache-from: type=registry,ref=${{ secrets.ECR_REGISTRY }}/${{ secrets.ECR_REPOSITORY }}:cache
cache-to: type=inline
 
-	name: Update ECS task and service
uses: aws-actions/amazon-ecs-deploy-task-definition@v2 with:
task-definition: path/to/ecs-task-def.json service: ${{ secrets.ECS_SERVICE }} cluster: ${{ secrets.ECS_CLUSTER }} wait-for-service-stability: true

name: CI — frontend & backend tests

on:
pull_request:
push:
branches: [ develop, feature/** ]

jobs:
frontend:
runs-on: ubuntu-latest steps:
-	uses: actions/checkout@v4
-	uses: actions/setup-node@v4 with: node-version: 20
-	name: Install deps
working-directory: frontend run: npm ci
-	name: Lint
working-directory: frontend run: npm run lint --if-present
-	name: Tests
working-directory: frontend
run: npm test --if-present	watchAll=false

backend:
runs-on: ubuntu-latest services:
postgres:
image: postgres:15 env:
POSTGRES_USER: postgres POSTGRES_PASSWORD: postgres ports: ['5432:5432']
options: >-
--health-cmd pg_isready
--health-interval 10s
--health-timeout 5s
--health-retries 5 steps:
-	uses: actions/checkout@v4
-	uses: actions/setup-node@v4
 
with: node-version: 20
-	name: Install deps
working-directory: backend run: npm ci
-	name: Lint
working-directory: backend run: npm run lint --if-present
-	name: Tests
working-directory: backend env:
DATABASE_URL: postgres://postgres:postgres@localhost:5432/postgres run: npm test --if-present
