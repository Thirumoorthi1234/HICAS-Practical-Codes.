
PROGRAM NO: 05 :React Components - State & Props	

App.jsx:
import React, { useState } from "react"; import "./App.css";

function Counter({ initialCount }) {
const [count, setCount] = useState(initialCount); const [animate, setAnimate] = useState(false);

const handleClick = (value) => { setCount(count + value); setAnimate(true);
setTimeout(() => setAnimate(false), 300); // reset animation
};


return (
<div className="counter-card">
<h2 className={`count ${animate ? "animate" : ""}`}>{count}</h2>
<div className="buttons">
<button onClick={() => handleClick(1)}>Increment</button>
<button onClick={() => handleClick(-1)}>Decrement</button>
<button onClick={() => setCount(initialCount)}>Reset</button>
</div>
</div>
);
}


function App() { return (
<div className="app-container">
<h1>Props + State Component</h1>
<Counter initialCount={0} />
 
</div>
);
}


export default App;


App.css:
/* Ensure background covers full page */ html, body, #root {
height: 100%;
margin: 0; color:red;
}


/* Full-page centered layout */
.app-container { height: 100vh; width: 100%;
background: linear-gradient(135deg, #7b2ff7, #f107a3, #ff9800); display: flex;
flex-direction: column;
justify-content: center; /* Vertical center */ align-items: center;	/* Horizontal center */ text-align: center;
}


.app-container h1 { color: white;
margin-bottom: 20px;
font-family: "Segoe UI", sans-serif;
text-shadow: 2px 2px 6px rgba(0,0,0,0.4);
}
 
/* Counter Card */
.counter-card { background: white; padding: 30px; border-radius: 20px;
box-shadow: 0 10px 25px rgba(0,0,0,0.25); text-align: center;
max-width: 350px;
}


/* Count Display */
.count {
font-size: 3rem; font-weight: bold; color: #7b2ff7; margin: 20px 0;
transition: transform 0.3s ease, color 0.3s ease;
}
.count.animate { transform: scale(1.3); color: #f107a3;
}


/* Buttons */
.buttons button { padding: 10px 20px; margin: 8px;
font-size: 1rem; border: none; border-radius: 8px; cursor: pointer;
 
color: #fff;
font-weight: bold; transition: opacity 0.3s ease;
}
.buttons button:nth-child(1) { background: #27ae60; /* Green */
}
.buttons button:nth-child(2) { background: #e74c3c; /* Red */
}
.buttons button:nth-child(3) { background: #3498db; /* Blue */
}
.buttons button:hover { opacity: 0.8;
}
